// let: 동적 타이핑(Dynamin typing)
// 변수가 가지는 고정 타입이 없다
// 하지만 타입이 없는 것은 아니다

//데이터 타입
//1. 기본타입(Primitive type)
// Boolean, null, undefined, number, string, symbol
//2. 객체(Objects)

let whatever = "Mark";

whatever = 37;

whatever = true;

console.log(whatever);
