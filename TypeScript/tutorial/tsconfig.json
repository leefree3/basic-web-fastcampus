{
    "include": [
        "src/**/*.ts"
    ],
    "exclude": [
        "node_modules"
    ],
    // 모듈 es6, target es6로 바꾸고 rebuild 하고 실행해보기
    // 모듈 commonjs, target es5로 바꾸고 rebuild하고 실행해보기
    // sourceMap: ts파일 원본을 읽고싶은지 여부 
    // removeComments: 브라우저 상에 주석제거
    // noImplicitAny: '함수의 파라미터의 타입을 지정하지 않으명 어떤 타입이던(any) 상관없이 쓸 수 있다'를 방지
    "compilerOptions": {
        "module": "es6",
        "rootDir": "src",
        "outDir": "dist",
        "target": "es6",
        "sourceMap": true,
        "removeComments": true,
        "noImplicitAny": false
    }
}